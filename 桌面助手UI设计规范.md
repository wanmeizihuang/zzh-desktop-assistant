# 桌面智能助手 · UI 设计规范（AI 实现用）

> 本文件是**与技术栈无关**的 UI 实现规格，供 AI 编码工具或开发者直接落地。
> 源方案：《桌面智能助手总体开发方案 v0.1》（2026-07-30）。
> 视觉参考：`桌面助手UI设计原型.html`（可在浏览器打开，含可点击交互）。
> 定稿风格：浅色轻玻璃工具界面 + 蓝色功能强调 +「晓曦」粉青身份色。
> 定稿形象：`xiaoxi_full.png`，收起态完整显示，不做圆形裁切。

---

## 0. 背景与硬约束

| 项 | 约束 |
|---|---|
| 首期平台 | Windows 10/11 x64（后续才评估 macOS/Linux） |
| 推荐技术栈 | Rust + Slint 原生 UI（方案 ADR-002）；本规范亦可映射到 HTML/CSS、Flutter 等 |
| 资源目标 | 收起态内存 ≤50MB，展开态 ≤80MB；冷启动到角色可见 ≤1.5s；点击展开反馈 ≤100ms、完整显示 ≤300ms |
| 采样 | 展开默认 1s，收起降频 2–5s；图表仅在可见时重绘 |
| 设计四原则 | **能力驱动**（UI 只展示连接器真实能力）、**降级友好**（不支持项明确说明，绝不伪造或留空）、**隐私优先**（文件发送前必须确认、敏感文件默认阻止）、**不抢焦点**（透明窗口、不自动弹窗） |
| 视觉禁忌 | 功能强调色**禁用紫色**；仅使用蓝色。晓曦角色自身的粉青配色不受此限制，但不得扩散为大面积 UI 底色 |

---

## 1. Design Tokens

### 1.1 颜色
```
--accent:        #2f6bff   /* 主强调蓝 */
--accent-soft:   #eaf1ff   /* 强调浅底 */
--accent-press:  #1f53d6   /* 按下态 */
--ok:            #28a96b   /* 正常/成功 */
--warn:          #e0a32e   /* 降级/提醒 */
--danger:        #e5484d   /* 告警/阻止 */
--ink:           #1c2433   /* 主文字 */
--ink-2:         #5a6577   /* 次级文字 */
--ink-3:         #8b94a6   /* 辅助文字 */
--line:          rgba(20,30,60,.08)        /* 分隔线/描边 */
--panel:         rgba(248,250,253,.96)     /* 高不透明轻玻璃面板 */
--surface:       rgba(255,255,255,.86)     /* 卡片/输入区 */
--xiaoxi-cyan:   #28c9d8   /* 角色身份点缀 */
--xiaoxi-pink:   #f2a0c2   /* 角色身份点缀 */
```

### 1.2 圆角 / 阴影 / 间距
```
圆角：  panel 14 · card 8 · pill 999 · control 8
阴影：  0 16px 40px -18px rgba(24,42,90,.24)  （克制，避免重投影）
间距刻度：4 / 8 / 12 / 16 / 24
```

### 1.3 字体
```
font-family: "Segoe UI Variable", "Microsoft YaHei UI", "PingFang SC", sans-serif
字号：标题 30 · 面板标题 14 · 卡片值 21 · 正文 13 · 辅助 11–12
监控数字使用等宽数字特性，数值更新不得推动相邻布局。
```

### 1.4 窗口背景

- 收起态窗口：完全透明，仅绘制晓曦和状态标记。
- 展开态面板：使用 `--panel` 高不透明浅色表面，不启用持续实时背景模糊。
- 原型展示页可继续使用浅色渐变背景，但该背景不属于桌面应用窗口。

---

## 2. 组件库（通用属性）

| 组件 | 关键属性 / 状态 | 说明 |
|---|---|---|
| **角色窗口**（收起态） | 透明无边框、固定尺寸 200×200、可拖动；晓曦完整显示 + 可选告警角标 | 无底板、姓名、提示或装饰圆点，不抢焦点 |
| **告警角标** | 右上角红色圆形「!」，脉冲动画 | CPU/内存/温度超阈值时出现 |
| **面板外壳** | 412×640、高不透明轻玻璃、圆角 14、顶部标题栏（角色头像 + 名称 + 控件：置顶/锁定/最小化/关闭） | 从角色相邻位置展开，页签切换不改变窗口尺寸 |
| **页签栏** | 监控 / 对话 / 设置 三段，选中态蓝色下划线 | 仅一个面板，切换内容 |
| **指标卡** | label（图标+名）、当前值、迷你趋势图（SVG polyline）、副文本 | 2 列网格 |
| **降级指标卡** | 特殊态：值区显示「当前硬件不支持」，warn 描边/底色，附原因 | 温度读不到时 |
| **网络指标卡** | 同时显示 ↓/↑ 速率 + 双线趋势图 | |
| **采样频率段控** | 1s / 2s / 5s 三段选择 | |
| **智能体切换 chip** | 横向滚动，选中填充蓝；含「+ 添加」 | 对话页签顶部 |
| **能力标签** | 流式/文件/图片/本地路径/工具调用，支持打 ✓ 或 ✗（红） | 能力驱动，按连接器真实能力渲染 |
| **对话气泡** | 用户（右，蓝底）/ 助手（左，玻璃底）；流式时光标闪烁 | |
| **停止生成按钮** | 红色描边，流式响应时出现 | |
| **待发送文件药丸** | 文件名 + 大小 + 「本地处理/上传云端」标记 | 发送前展示 |
| **输入栏** | 多行文本框 + 附件按钮 + 发送按钮 | |
| **连接器行**（设置） | 图标 + 名称 + 类型 + 能力标签 + 状态（已配置/需配置）+ 编辑 | |
| **开关 Switch** | 关(灰)/开(蓝)，圆形滑块 | |
| **阈值标签** | CPU>85% / 内存>90% / 温度>80°C 小标签 | |
| **确认弹层** | 半透明遮罩 + 居中白卡：目标服务 + 文件列表 + 风险说明 + 取消/确认 | 文件发送前 |
| **托盘菜单** | 头部(角色+状态) + 菜单项(图标+文字+可选状态) + 分隔线 + 危险项(退出) | 常驻不抢焦点 |

---

## 3. 逐屏规格

### 屏 1 — 收起态角色窗口
- 用途：长期驻留桌面，最小打扰。
- 结构：`角色窗口`(200×200 透明) → `xiaoxi_full.png`（完整显示，等比 `contain`）+ 可选 `告警角标`。
- 状态：默认态 / 告警态（右上角红色角标）；不在收起态常驻显示状态文字。
- 约束：不抢焦点；锁定后禁止拖动。

### 屏 2 — 展开面板 · 监控页签
- 尺寸：固定 412×640；可用工作区高度不足时缩短面板并启用内部滚动。
- 结构：`面板外壳` → `页签栏(监控 active)` → 6 张 `指标卡`（2 列 × 3 行）：
  1. CPU 总占用 23%
  2. 内存 8.2/16 GB（51%）
  3. 显存 2.1/8 GB（26%）
  4. GPU 利用率 14%
  5. **温度（降级态）**：值区「当前硬件不支持」，warn 样式，副文本「可选传感器桥接未安装」
  6. 网络 ↓12.4 MB/s ↑1.8 MB/s（双线趋势）
- 底部：`监控页脚`（采样频率 1s · 阈值提醒已开启）+ `采样频率段控`。
- 降级规则：温度读不到 → 显示「当前硬件不支持」+ 原因，**绝不伪造或用负载估算**。

### 屏 3 — 展开面板 · 对话页签
- 结构：`智能体切换 chip`(Codex 选中) → `能力标签`(流式✓ 文件✓ 本地路径✓ 图片✗ 工具调用✗) → 用户气泡 → 助手气泡(流式, 光标闪烁) → `停止生成按钮` → `待发送文件药丸`(memory-leak.go · 本地处理) → `输入栏`。
- 布局：智能体和能力区固定在顶部，消息区占据剩余空间并虚拟化滚动，文件队列和输入栏固定在底部。
- 交互：流式响应中显示停止；文件先入待发送队列，发送前弹确认层。

### 屏 4 — 文件发送确认层
- 触发：点击发送含文件时，覆盖在面板上。
- 结构：`遮罩`(半透明+轻模糊) → `确认卡`：
  - 标题「确认发送文件」
  - `目标服务`：千问 · 🔒 https://dashscope.aliyuncs.com · 厂商官方 HTTP
  - `文件列表`：
    - 季度报告.pdf · 2.1 MB · 标记「上传云端」
    - **.env · 0.3 KB · 标记「已阻止」**（敏感凭据，默认阻止）
  - `风险说明`：云端文件不会本地留存；.env 等敏感扩展名一律不发送
  - 按钮：取消 / 确认发送 1 个文件
- 规则：敏感扩展名（.env / 私钥 / 凭据库）默认阻止；目标服务可视化（URL + 协议锁标）。

### 屏 5 — 展开面板 · 设置页签
- 布局：内容区域内部滚动，连接器编辑使用独立弹层，不改变主面板尺寸。
- 结构：
  - **智能体连接器**（列表，每行列能力与状态）：
    - Codex（本地 CLI）流式/文件/本地路径 · 已配置
    - 千问（厂商官方 HTTP）流式/文件/图片 · 已配置
    - 豆包（厂商官方 HTTP）流式/图片 · 需配置
    - DeepSeek（OpenAI 兼容）流式 · 已配置
    - Hermes（兼容 API）流式/文件 · 需配置
    - WorkBuddy（本地 CLI）流式/文件/本地路径 · 需配置
  - **监控设置**：采样频率段控、阈值提醒开关、阈值标签(CPU>85%/内存>90%/温度>80°C)、可选温度桥接(未安装·点击安装)
  - **窗口与隐私**：置顶 / 锁定 / 开机启动 / 日志不记录正文 开关

### 屏 6 — 托盘右键菜单
- 结构：`头部`(晓曦头像 + 「桌面助手 / 晓曦 · 监控中`) → 菜单项：
  - 显示/隐藏（状态：显示）
  - 置顶（关）
  - 锁定位置（关）
  - 暂停监控（运行中）
  - 分隔线
  - 设置
  - 退出（危险项，红色）
- 约束：全程不抢夺其他应用焦点。

---

## 4. 状态与降级规则（必须实现）

1. **温度不支持** → 单项显示「当前硬件不支持」，其余监控继续工作；不伪造。
2. **传感器桥接崩溃** → 标记数据过期，退避重启（≤3 次）。
3. **API 超时/限流** → 保留输入与附件列表，显示可操作错误，遵循 `Retry-After`。
4. **CLI 不存在/版本不兼容** → 配置检测给出路径、版本、能力诊断，不影响其他连接器。
5. **网络断开** → 云端智能体不可用，本地监控与本地 CLI 保持可用。
6. **敏感文件** → .env / 私钥 / 凭据库 默认阻止发送。
7. **超阈值** → 收起态出现红色角标。
8. 所有外部错误映射到内部错误码（如 `AUTH_INVALID` / `RATE_LIMITED` / `FILE_UNSUPPORTED` / `CLI_NOT_FOUND`），UI 不依赖厂商原始报错文本。

---

## 5. 交互行为

- 点击角色 → 原位展开面板（≤100ms 反馈，≤300ms 完整显示）；靠近屏幕边缘自动向可见区域展开。
- 面板与角色保持 8px 间距；根据可用空间向左或向右展开，上下位置夹取到工作区。
- 页签切换 监控/对话/设置（单面板内容切换，外部尺寸保持 412×640）。
- 文件拖入 → 直接展开文件队列，**不立即上传**；确认后才上传/传路径。
- 托盘：显示/隐藏、置顶、锁定、暂停监控、设置、退出。
- 多显示器：按设备 ID + 逻辑坐标 + DPI 保存位置；显示器移除恢复到主屏可见区。

---

## 6. 资源清单

| 资产 | 路径 | 说明 |
|---|---|---|
| 收起态「晓曦」 | `xiaoxi_full.png` | 1009×968 RGBA 真透明图片；完整等比显示，不裁切 |
| 备选形象（7 款） | `mascots/` 其余 PNG + `桌面助手形象预览.html` | 机械猫/狐/熊猫/狼、能量光球、晓曦、糖糖 |
| 视觉参考原型 | `桌面助手UI设计原型.html` | 可点击交互的高保真原型 |
| 源方案 | `docs/plans/2026-07-30-desktop-assistant-design.md` | 完整需求与约束 |

> `xiaoxi_full.png` 已验证为 RGBA，四角 Alpha 为 0。打包前应将完全透明像素的隐藏 RGB 清零，避免缩放采样产生边缘杂色；该处理不得改变任何可见像素。

---

## 7. 验收对照（方案条款）

- [ ] 收起态仅完整晓曦角色，无圆形裁切、文字、底板或装饰圆点；点击后相邻展开（方案 6.1）
- [ ] 展开面板保持 412×640，页签切换不改变外部尺寸
- [ ] 不使用持续实时背景模糊，低资源目标保持有效
- [ ] 六指标 + 趋势图 + 采样/阈值（方案 6.2）
- [ ] 温度不支持明确说明（方案 6.2 / ADR-005）
- [ ] 能力驱动 UI、流式 + 停止（方案 6.3 / ADR-003）
- [ ] 文件发送前确认、目标可视化、敏感阻止（方案 6.4 / 第 9 节）
- [ ] 连接器列表 + 能力标签 + 窗口/隐私设置（方案 6.5 / ADR-004）
- [ ] 托盘菜单与方案 6.1 一致
- [ ] 密钥不明文落盘、日志脱敏（方案 6.5 / 第 9 节）

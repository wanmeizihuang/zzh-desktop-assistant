# P2 桌面外壳第二批验证报告

## 范围

- 版本化本地 JSON 配置。
- 窗口位置、始终置顶和位置锁定的自动保存与启动恢复。
- 同目录临时文件、落盘刷新和 Windows 原子替换。
- 损坏配置保留与默认值恢复。
- 300ms 防抖写入和退出前最后一次强制落盘。

配置路径：`%LOCALAPPDATA%/Xiaoxi Desktop Assistant/settings.json`。

## 自动化结果

| 检查 | 结果 |
|---|---|
| `cargo test --workspace --offline` | 52 个测试通过 |
| 配置缺失与默认值 | 通过 |
| JSON 完整往返 | 通过 |
| 损坏文件改名保留 | 通过 |
| Windows 原子替换 | 通过 |
| 防抖队列退出落盘 | 通过 |
| 严格 Clippy | 通过 |
| release 构建 | 通过 |

## Windows 跨进程验证

环境为 Windows 11 x64、150% DPI。

| 场景 | 结果 |
|---|---|
| 收起/展开物理尺寸 | 300 x 300 / 618 x 960 |
| 保存测试状态 | 置顶关闭、位置锁定开启 |
| 退出后配置 | 测试状态完整落盘 |
| 重启后顶层标志 | 保持关闭 |
| 重启后窗口物理位置 | 恢复为 195,255 |
| 恢复默认行为 | 置顶开启、位置锁定关闭并落盘 |
| ESC 收起 | 恢复为 300 x 300 物理尺寸 |

## 已知限制

- 当前只定义配置版本 1，后续字段变更时再增加单向迁移函数。
- 保存的是物理坐标；显示器移除和混合 DPI 下的最近工作区恢复属于 P2 Task 5。
- 托盘和单实例尚未接入，隐藏/恢复状态暂时只有核心状态测试。

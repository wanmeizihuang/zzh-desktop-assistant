# P2 桌面外壳第五批验证报告

## 范围

- 主窗口从 Windows 任务栏隐藏，通知区托盘保留为常驻入口。
- 收起态继续允许只保留 32 个物理像素可见。
- 展开态强制完整窗口位于最近显示器工作区内，避免边缘裁切。

## 备份

修改前提交 `3c72131` 已备份为唯一文件：`backups/p2-task5-3c72131.zip`。压缩包包含源码快照和对应 release 可执行文件。旧备份目录和旧备份标签已删除，新标签 `backup/p2-task5-20260730` 指向 `3c72131`。

## 自动化结果

| 检查 | 结果 |
|---|---|
| `cargo test --workspace --offline` | 67 项通过，1 项隔离 HKCU 测试按设计默认忽略 |
| `cargo clippy --workspace --all-targets --offline -- -D warnings` | 通过 |
| `cargo fmt --all -- --check` | 通过 |
| `cargo build --release -p desktop-assistant --offline` | 通过 |
| `git diff --check` | 通过 |
| 收起态边缘可见策略 | 通过 |
| 展开态完整工作区钳制 | 通过 |

## Windows 交互结果

环境为 Windows 11 x64、150% DPI。

| 场景 | 结果 |
|---|---|
| 任务栏应用按钮 | Windows 任务栏自动化树中晓曦按钮数量为 0 |
| 应用窗口 | 进程和窗口保持响应，托盘实现未变更 |
| 边缘测试位置 | 收起态移动至工作区右下角 `(2488, 1576)`，仅保留 32 像素可见 |
| 展开后物理尺寸 | `618 x 960`，对应 150% DPI 下的 `412 x 640` 逻辑尺寸 |
| 展开后窗口矩形 | `(1902, 648)-(2520, 1608)` |
| 完整可见 | 四条边均位于工作区 `(0, 0)-(2520, 1608)` 内 |
| 测试清理 | 已收起并恢复测试前窗口位置 |

## 行为说明

收起态仍适合贴边放置。展开时如果右侧或底部空间不足，整个面板会向工作区内部移动；收起后保持调整后的当前位置，不额外维护第二套隐藏锚点。

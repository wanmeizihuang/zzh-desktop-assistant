# P2 桌面外壳最终关闭报告

日期：2026-07-31
结论：P2 已关闭，P3 已启动。

## 已交付

- 收起、展开、隐藏、恢复和退出共用一套生命周期状态。
- 支持拖动、位置锁定、始终置顶、ESC 收起和边缘展开位置恢复。
- 主窗口使用工具窗口样式，不在任务栏显示。
- 托盘支持显示/隐藏、展开/收起、设置、行为开关和退出。
- 单实例唤醒、当前用户开机启动、版本化配置和损坏配置恢复已接通。
- 显示器工作区枚举、最近屏幕选择、屏外夹取和 DPI 变化恢复已实现。
- 默认动态形象、形象切换、ZZH 品牌和程序图标已接入。

## 最终质量门禁

| 检查 | 结果 |
|---|---|
| `cargo fmt --all -- --check` | 通过 |
| `cargo test --workspace --offline` | 71 项通过，1 项隔离 HKCU 写入测试按设计忽略 |
| `cargo clippy --workspace --all-targets --offline -- -D warnings` | 通过 |
| `cargo build --release -p desktop-assistant --offline` | 通过 |
| 托盘隐藏/显示三轮回归 | 通过，进程持续存活，无新增崩溃 |
| 任务栏隐藏与工具窗口样式 | 通过 |

## 资源样本

对项目根目录中已连续运行约 5 小时的旧发布程序采集 15 秒样本：工作集 `101.96 MiB`、私有内存 `54.8 MiB`、CPU `1.56%` 单核（16 逻辑处理器下全机约 `0.098%`）。该样本包含动态形象且不是本次刚生成的 Release 构建，因此只作为趋势参考，不作为 P3 最终性能结论。

## 已接受的后续项

- 在真实双屏和 100%/150% 混合缩放环境执行热插拔与跨屏位置验证，纳入 P6 兼容矩阵。
- 在 P3 完成收起态采样降频、动画刷新优化和长期资源基线，目标仍以低资源常驻为准。
- 24 小时内存趋势与 ADR-002 技术栈最终决策继续按既有安排在稳定化阶段复核。

这些项目不阻断 P2 关闭，但必须在对应后续阶段报告中保留并验证。
